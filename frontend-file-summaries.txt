{
  "frontend/src/App.tsx": "Main app with TanStack Router; adds the `/products/$storeId/$productId` route mapped to the new `ProductDetail` page component alongside all existing routes.",
  "frontend/src/pages/Checkout.tsx": "Checkout page with shipping and order review steps; fixed `ProductType`/`ProductStatus` casting and passes `{items, successUrl, cancelUrl}` object to `createCheckoutSession.mutateAsync`.",
  "frontend/src/pages/OrderConfirmation.tsx": "Order confirmation page using status prop for OrderStatusTimeline (not currentStatus); uses number-based formatDate/formatPrice.",
  "frontend/src/pages/MyOrders.tsx": "My orders page using useNavigate with typed params instead of template-literal Link to props, fixing TanStack Router type errors.",
  "frontend/src/pages/VendorRegistration.tsx": "Vendor registration page that checks approval status, shows VendorRegistrationForm for new applicants, a pending state after submission, and a redirect for already-approved vendors.",
  "frontend/src/pages/VendorDashboard.tsx": "Vendor dashboard page; replaced `useMyStores` with `useGetStoresByVendor(vendorPrincipal)`, changed `selectedStoreId` state type to `string | undefined`, and updated `StoreSelector` to use `onSelect` prop.",
  "frontend/src/pages/VendorProducts.tsx": "Vendor products page guarded by auth and approval; replaced `ProductListManager` (which expected `vendorId`) with `StoreProductManager` (which expects `storeId` and `vendorId`).",
  "frontend/src/pages/AdminDashboard.tsx": "Admin-only dashboard page that now includes `AuctionManagementPanel` and `TradeOfferManagementPanel` sections after the existing Payout Management section.",
  "frontend/src/components/vendor/VendorRegistrationForm.tsx": "Vendor registration form that stores the vendor ID in localStorage and requests approval; removed the `useCreateVendorProfile` stub call (which had an incompatible `id` field) and calls only `useRequestApproval`.",
  "frontend/src/components/layout/Header.tsx": "Sticky header with logo, nav links, auth dropdown, and mobile menu; uses `/my-orders` route (not `/orders`) for the My Orders navigation link.",
  "frontend/src/utils/stripe.ts": "Stripe utility module exporting the CheckoutSession type, URL builder helpers (buildSuccessUrl, buildCancelUrl), and a parseCheckoutSession function that validates the JSON response from the backend.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend interaction; exports `LocalProduct` type with number fields and `backendProductToLocal` conversion, `useSearchProducts` now returns `LocalProduct[]` with proper client-side filtering applied, and all other hooks (stores, cart, orders, payouts, Stripe, auctions, trade offers) are preserved.",
  "frontend/src/components/checkout/OrderReview.tsx": "Order review component that displays cart items with product images, titles, variant labels, and variant-adjusted prices; calculates the accurate order total; conditionally shows shipping address or digital delivery note; and renders a Pay with Stripe button that triggers the Stripe redirect.",
  "frontend/src/pages/PaymentSuccess.tsx": "Payment success page; changed `sessionId` from `null` to `undefined` to match `useGetStripeSessionStatus` parameter type.",
  "frontend/src/pages/PaymentCancel.tsx": "Payment cancellation page that informs the user their payment was cancelled without charging them, shows the order reference if available, and provides buttons to return to cart or continue shopping.",
  "frontend/src/components/orders/OrderStatusBadge.tsx": "Order status badge importing OrderStatus from local types/index.ts instead of backend.d.ts.",
  "frontend/src/components/orders/OrderStatusTimeline.tsx": "Order status timeline importing OrderStatus from local types/index.ts; accepts status prop (not currentStatus).",
  "frontend/src/components/admin/OrderManagementPanel.tsx": "Admin order management panel that lists all orders with status filtering and per-row status update dropdowns; fixed import path for OrderStatusBadge to use the correct relative path from orders directory.",
  "frontend/src/utils/urlParams.ts": "URL and session storage utility module; adds the missing `getSessionIdFromUrl` export that extracts the Stripe `session_id` query parameter from the current URL, alongside all existing helpers.",
  "frontend/src/components/products/SearchFilters.tsx": "Search filters importing SearchFilter, ProductType, and Variant_quantityDesc_priceDesc_priceAsc from local types; uses onFilterChange prop (not onChange).",
  "frontend/src/components/vendor/ProductListManager.tsx": "Vendor product list with edit/delete actions and add-product dialog; imports Product type from local types and uses number-based price formatting.",
  "frontend/src/pages/Home.tsx": "Home page with hero section, feature highlights, and featured products grid; uses `useSearchProducts` returning `LocalProduct[]` and converts to local `Product` type via `localToProduct` (mapping `storeId` to `vendorId` for correct navigation).",
  "frontend/src/components/admin/PayoutStatusBadge.tsx": "New reusable badge component that renders color-coded status badges for PayoutStatus values (amber=pending, blue=processing, green=completed, red=failed), consistent with the OrderStatusBadge design pattern.",
  "frontend/src/components/admin/PayoutManagementPanel.tsx": "New admin panel component displaying all payouts in a sortable table with Payout ID, Vendor, Order ID, Gross/Commission/Net amounts, status badges, and per-row status update dropdowns; includes a commission rate editor section at the top with current rate display and save button.",
  "frontend/src/components/vendor/VendorPayoutsPanel.tsx": "Vendor payouts panel showing earnings summary cards and payout history table; updated to use `useGetVendorPayouts()` with no arguments and `LocalPayout` type, with `vendorId` prop prefixed as unused.",
  "frontend/src/components/vendor/VendorOrderHistory.tsx": "Vendor order history table showing transaction details with sortable date column and receipt navigation; fixed route path from `/order/$orderId/receipt` to `/orders/$orderId/receipt` and uses `VendorTransaction` type.",
  "frontend/src/pages/OrderReceipt.tsx": "Order receipt page importing OrderStatus from local types; uses number-based formatDate/formatPrice helpers.",
  "frontend/src/index.css": "Global styles with OKLCH luxury color system for light/dark modes; adds a comprehensive @media print block that hides navigation/header/footer/buttons, resets page margins, forces print-friendly colors for gold/bronze tokens, and ensures the invoice layout renders cleanly when printed or saved as PDF.",
  "frontend/src/components/trade/TradeOfferCard.tsx": "Trade offer card showing offer details with accept/reject/cancel/counter actions; updated to use `LocalTradeOffer` type from useQueries.",
  "frontend/src/pages/AuctionDetail.tsx": "Auction detail page with countdown timer, bid form, and bid history; fixed `typedAuction.auctionId` to `typedAuction.id` and `auctionId` param passed as `undefined` instead of `null`.",
  "frontend/src/pages/TradeOffers.tsx": "Trade offers page with incoming/outgoing tabs; fixed field references from `targetPrincipal`/`offeredBy` to `recipientId`/`offererId`, removed unsupported `perspective`/`isLoading` props from `TradeOfferGrid`.",
  "frontend/src/pages/NewTradeOffer.tsx": "New trade offer page without currentUserId prop passed to NewTradeOfferForm.",
  "frontend/src/components/admin/TradeOfferManagementPanel.tsx": "Admin panel listing all trade offers in a sortable table (by status or creation date) with Cancel action for pending offers and truncated principal display.",
  "frontend/src/components/vendor/StoreSelector.tsx": "Dropdown component for switching between vendor stores; updated to use `store.id` (not `store.storeId`) for both the Select value and item keys, ensuring it works with the actual `StoreResponse` backend type.",
  "frontend/src/components/vendor/StoreFormModal.tsx": "Dialog-based form modal for creating or editing a store; updated to use the correct `StoreResponse` field names (`id`, `contactInfo` instead of `storeId`, `contactEmail`, `logoUrl`), simplified to a single `store` prop (instead of `mode`+`initialData`), and wires `useCreateStore`/`useUpdateStore` with the correct payload shape matching the backend interface.",
  "frontend/src/components/vendor/StoreListManager.tsx": "Lists vendor stores in a card grid with edit/activate actions; manages `StoreFormModal` open state internally with `isOpen`/`onClose` props, and fetches stores via `useGetStoresByVendor` using the current user's principal.",
  "frontend/src/components/vendor/ProductForm.tsx": "Vendor product creation/editing form using local ProductType and Product types from types/index.ts; handles create and update mutations with price/stock validation.",
  "frontend/src/components/vendor/VendorAuctionsPanel.tsx": "Vendor auctions panel with collapsible create form and auction rows; fixed all `auction.auctionId` references to `auction.id` and removed the unsupported `productId` prop from `CreateAuctionForm`.",
  "frontend/src/components/vendor/VendorProfileEditor.tsx": "Vendor profile editor form for updating store name, description, and contact info; removed dependency on `useUpdateVendorProfile` stub and handles updates locally with toast feedback.",
  "frontend/src/components/auctions/AuctionCard.tsx": "Auction card component displaying auction details (title, status, bids, price, end time) with a bid/view button; updated to use `LocalAuction` type from useQueries.",
  "frontend/src/components/auctions/AuctionGrid.tsx": "Auction grid component that renders a responsive grid of auction cards; updated to use `LocalAuction` type from useQueries instead of the local `Auction` type.",
  "frontend/src/components/auctions/BidForm.tsx": "Bid form using local Auction type; removed unsupported onBidPlaced prop, uses amount field (not bidAmount) for usePlaceBid.",
  "frontend/src/components/products/ProductGrid.tsx": "Product grid that renders a responsive grid of `ProductCard` components with loading skeletons, empty state, and null-safety filtering on the products array.",
  "frontend/src/components/trade/NewTradeOfferForm.tsx": "New trade offer form with offered/requested item lists and cash adjustment; updated to pass correctly-typed objects `{ productId, quantity: bigint }` matching the `useCreateTradeOffer` mutation signature.",
  "frontend/src/pages/Auctions.tsx": "Auctions listing page; removed the unsupported `isLoading` prop from `AuctionGrid` and handles loading state inline with skeleton cards.",
  "frontend/src/pages/Cart.tsx": "Cart page that fetches product details for cart items; fixed `ProductType`/`ProductStatus` enum type casting using `as unknown as ProductType/ProductStatus` pattern.",
  "frontend/src/pages/ProductListing.tsx": "Product listing page with search filters and product grid; uses `useSearchProducts` with a `SearchFilter` state and converts `LocalProduct[]` to local `Product[]` via `localToProduct` with correct `storeId`→`vendorId` mapping.",
  "frontend/src/components/checkout/ShippingAddressForm.tsx": "Shipping address form with client-side validation for all required fields (street, city, state, zip, country), inline error messages, and a skipShipping mode that auto-bypasses address input for digital-only orders.",
  "frontend/src/components/vendor/StoreProductManager.tsx": "Store product manager with add/edit/delete functionality and variant support; refactored to use a `DisplayProduct` intermediate type and explicit backend↔display conversion functions to avoid type conflicts between local and backend Product types.",
  "frontend/src/components/admin/ApprovalDashboard.tsx": "Admin approval dashboard that lists all vendor approval requests with Approve/Reject buttons; now tracks per-row loading state via `processingPrincipal` so only the active row's buttons show a spinner and are disabled, and shows descriptive success/error toast messages on mutation completion.",
  "frontend/src/components/vendor/CreateAuctionForm.tsx": "Vendor auction creation form; fixed to only pass `productId`, `startingPrice`, and `endTime` to `createAuction.mutateAsync` matching the stub's type signature, with `vendorId` prop prefixed as unused (`_vendorId`).",
  "frontend/src/types/index.ts": "Local type definitions with `Product` (number fields), `ProductVariant`, `SearchFilter`, `CartItem`, `Order`, `Payout`, `Auction`, `TradeOffer`, and related enums/types; all numeric fields use `number` (not `bigint`).",
  "frontend/src/components/products/ProductCard.tsx": "Product card component that auto-selects the first variant on mount, dynamically updates the displayed price (with strikethrough base price for adjusted variants), stock count, out-of-stock overlay, and Add to Cart button state whenever a variant is chosen from the inline selector; all changes are local to each card instance.",
  "frontend/src/pages/ProductDetail.tsx": "Product detail page that auto-selects the first variant on load, dynamically updates the displayed price and stock whenever a variant is selected (no backend call needed), shows a strikethrough base price when a variant has a price adjustment, displays an out-of-stock notice and disables the Add to Cart button when the selected variant has zero stock.",
  "frontend/src/components/trade/TradeOfferGrid.tsx": "Trade offer grid component rendering a list of trade offer cards; updated to use `LocalTradeOffer` type from useQueries instead of the local `TradeOffer` type.",
  "frontend/src/components/trade/CounterOfferForm.tsx": "Counter offer form for responding to a trade offer with swapped items and cash adjustment; updated to use `LocalTradeOffer` type and pass correctly-typed objects to `useCounterTradeOffer`.",
  "frontend/src/components/cart/CartItemRow.tsx": "Cart item row component that accepts a resolved Product prop, displays the product title, thumbnail image, variant label, per-unit effective price (base + variant adjustment), and line total, with a remove button that calls useRemoveFromCart.",
  "frontend/src/components/cart/CartSummary.tsx": "Cart summary component that renders CartItemRow for each item with resolved product data, calculates accurate subtotals by applying variant price adjustments, and shows an order summary panel with a Proceed to Checkout button."
}